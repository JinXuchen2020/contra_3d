<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <RootNamespace>Contra3D.Core</RootNamespace>
    <LangVersion>9.0</LangVersion>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="*.cs" />
    <Compile Include="../AI/*.cs" />
    <Compile Include="../Combat/*.cs" />
    <Compile Include="Playtest/*.cs" />
    <!-- Exclude nested Playtest/Playtest/ duplicates (migration artifact) -->
    <Compile Include="Playtest/Playtest/*.cs" Exclude="Playtest/Playtest/*.cs" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Contra3D.Core.Tests" />
    <InternalsVisibleTo Include="Contra3D.Core.Playtest" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Text.Json" Version="8.0.5" />
  </ItemGroup>

</Project>
